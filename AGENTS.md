### 코드 작업 및 Git 커밋 규칙

* 요청한 기능 추가 및 코드 수정을 수행한다.
* 작업 완료 후 변경 사항과 테스트 결과를 간단히 요약한다.
* 변경 사항을 바탕으로 적절한 Git 커밋 메시지를 한국어로 제안한다.
* 커밋 메시지는 `타입: 변경 내용 요약` 형식을 사용한다.
* 타입은 기능추가, 버그수정, 코드개선, 문서수정, 테스트, 작업정리, 스타일수정, 성능개선 중 적절한 것을 사용한다.
* 커밋 메시지는 개발자가 아닌 나도 쉽게 이해할 수 있도록 작성한다.
* 내가 직접 검토하고 커밋할 수 있도록 변경된 파일과 주요 내용을 알려준다.
* **내가 명시적으로 요청하지 않는 한 git commit, git push, git merge를 실행하지 않는다.**
* 기존의 사용자 변경 사항을 임의로 수정하거나 커밋하지 않는다.

# Codex 작업 지침

## 파일 수정 제한

이번 작업에서 Codex는 다음 두 파일만 수정할 수 있다.

- `social_worker.py`
- `admin_web.html`

### 절대 수정하지 말 것

위에 명시된 두 파일을 제외한 모든 파일은 수정하지 않는다.

특히 다음 작업을 하지 않는다.

- 다른 파일의 코드 수정
- 다른 파일의 내용 삭제
- 다른 파일의 이름 변경
- 다른 파일 삭제
- 새로운 파일 생성
- Git checkout/reset/revert 등의 명령으로 다른 파일의 상태 변경

## 작업 원칙

1. 문제가 다른 파일에 있다고 판단되더라도 해당 파일을 직접 수정하지 않는다.
2. 다른 파일의 수정이 필요하다면 작업을 중단하고 사용자에게 먼저 알린다.
3. `social_worker.py`와 `admin_web.html`의 기존 기능은 최대한 유지한다.
4. 요청받은 문제를 해결하기 위해 필요한 최소한의 부분만 수정한다.
5. 작업이 끝난 후 변경한 파일 목록을 확인하고 보고한다.